# -*- coding: utf-8 -*-
"""ML_Project_Machine Learning Task: House Price Prediction (Regression).ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1rjKiNtI-oS6D9Ooxvlq7rnizgaC7HrcJ

#**Exploratory Data Analysis of House Price Prediction Dataset**

#**Problem Statement**
The real estate market requires accurate estimation of property prices based on various property characteristics. This project aims to analyze a housing dataset containing features such as square footage, number of bedrooms, bathrooms, lot size, garage size, neighborhood quality, and year built.

Exploratory Data Analysis (EDA) is performed to understand the dataset structure, identify patterns, detect anomalies, and determine relationships between features and house prices. The insights obtained will help in building an effective machine learning regression model for predicting house prices.

#**Project Objectives**
The objectives of this project are:

* Understand the dataset structure and variables

* Perform data wrangling and cleaning

* Conduct univariate, bivariate, and multivariate analysis

* Identify patterns, trends, and anomalies

* Summarize insights for decision-making

* Provide conclusions and recommendations

#**Import libraries**
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

"""#**Load Data**"""

data=pd.read_csv('/content/house_price_regression_dataset2.csv')
data

"""#**Understand variables**
Dataset information:

Total rows: 1000

Total columns: 8

Dataset type: Structured numerical dataset

#**basic Checks**
"""

#find numbers of rows and columns
data.shape

#print last 5 rows
data.tail()

#print first 5 rows
data.head()

#check info
data.info()

#check data types
data.dtypes

#print all columns
data.columns

#print numerical columns
num_data=data.select_dtypes(include=['int64','float64'])
num_data

#print all categorical columns
cat_data=data.select_dtypes(include='object')
cat_data.columns

#get statistical report for numerical data
num_data.describe()

"""#**Data cleaning**"""

#missing values
data.isnull().sum()

"""There is no missing values"""

#check duplicates
data.duplicated().sum()

"""There is 0 duplicate

#**Exploratory Data Analysis**

**Check the distribution of House Price**
"""

plt.figure(figsize=(5,2))
sns.histplot(data, x='House_Price', kde=True, color='green')
plt.show()

"""**Insights**

The distribution shows how house prices vary across the dataset.

Most houses are concentrated within a moderate price range.

A few houses have very high prices, indicating possible luxury properties.

**Check the distribution of Square Footage**
"""

plt.figure(figsize=(5,2))
sns.histplot(data,x='Square_Footage',kde=True,color='green')
plt.show()

"""**Insights**

Most houses fall within a mid-range of square footage.

Very small and very large houses are fewer in number.

Square footage variation indicates diversity in property sizes.

**Check distribution of Lot Size**
"""

plt.figure(figsize=(5,2))
sns.histplot(data,x='Lot_Size',kde=True,color='green')
plt.show()

"""**Insights**

Majority of houses have moderate land sizes.

Few properties have extremely large lot sizes.

Lot size distribution helps understand property space availability.

**Check distribution of Bedrooms**
"""

plt.figure(figsize=(5,2))
sns.countplot(data,x='Num_Bedrooms')
plt.show()

"""**Insights**

Most houses have a moderate number of bedrooms.

Houses with very high bedroom counts are rare.

Bedroom count reflects typical residential property structures.

**Check distribution of Bathrooms**
"""

plt.figure(figsize=(5,2))
sns.countplot(data,x='Num_Bathrooms')
plt.show()

"""**Insights**

Bathrooms generally increase with house size.

Most houses have standard bathroom counts.

Few houses contain higher bathroom counts.

**Check distribution of Garage Size**
"""

plt.figure(figsize=(5,2))
sns.countplot(data,x='Garage_Size')
plt.show()

"""**Insights**

Many houses provide parking space through garages.

Some houses may not have large garage capacity.

Garage size indicates vehicle accommodation capacity.

**Check distribution of Neighborhood Quality**
"""

plt.figure(figsize=(5,2))
sns.countplot(data,x='Neighborhood_Quality')
plt.show()

"""**Insights**

Neighborhood quality varies across different properties.

Most houses fall within medium quality neighborhoods.

Few houses belong to extremely high or low quality areas.

#**Univariate Analysis**

**Histogram –Year Built**
"""

plt.figure(figsize=(5,2))
sns.histplot(data,x='Year_Built',kde=True,color='purple')
plt.show()

"""**Insights**

Majority of houses have moderate land sizes.

Few properties have extremely large lot sizes.

Lot size distribution helps understand property space availability.

#**Bivariate Analysis**

**Square Footage vs Price**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Square_Footage',y='House_Price')
plt.show()

"""**Insights**

Houses with larger square footage tend to have higher prices.

There is a positive relationship between house size and price.

Bigger houses generally attract higher market value.

**Lot Size vs Price**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Lot_Size',y='House_Price')
plt.show()

"""**Insights**

Houses with larger lot sizes tend to cost more.

Land availability contributes to property value.

Properties with small lots usually have lower prices.

**Bedrooms vs Price**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Num_Bedrooms',y='House_Price')
plt.show()

"""**Insights**

Houses with more bedrooms usually have higher prices.

Bedroom count influences property demand.

Larger families often prefer houses with more bedrooms.

**Bathrooms vs Price**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Num_Bathrooms',y='House_Price')
plt.show()

"""**Insights**

Houses with more bathrooms often have higher prices.

Bathroom availability improves property convenience.

Premium houses usually contain multiple bathrooms.

**Garage Size vs Price**
"""

plt.figure(figsize=(5,2))
sns.barplot(data,x='Garage_Size',y='House_Price')
plt.show()

"""**Insights**

Houses with larger garage space tend to have higher prices.

Garage capacity adds value to a property.

Houses without garages may be comparatively cheaper.

**Neighborhood Quality vs Price**
"""

plt.figure(figsize=(5,2))
sns.barplot(data,x='Neighborhood_Quality',y='House_Price')
plt.show()

"""**Insights**

Houses in better neighborhoods have higher prices.

Location plays a major role in property valuation.

Poor neighborhood quality often leads to lower house prices.

**Year Built vs Price**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Year_Built',y='House_Price')
plt.show()

"""**Insights**

Newly built houses generally have higher prices.

Older houses may have slightly lower prices due to aging.

Property age can influence maintenance and value.

**Square Footage vs Bedrooms**
"""

plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Square_Footage',y='Num_Bedrooms')
plt.show()

"""**Insights**

Larger houses usually have more bedrooms.

There is a positive relationship between house size and bedroom count.

Bigger floor area allows more room configurations.

#**Correlation Heatmap**

**Full Correlation Heatmap**
"""

plt.figure(figsize=(8,5))
sns.heatmap(data.corr(),annot=True,cmap='coolwarm')
plt.show()

"""**Insights**

The heatmap shows relationships between all numerical features.

Strong correlations highlight important predictors of house price.

Weak correlations indicate less influence on house price.

**Bedrooms Related Correlation**
"""

plt.figure(figsize=(6,4))
sns.heatmap(data[['Num_Bedrooms','Num_Bathrooms','House_Price']].corr(),annot=True)
plt.show()

"""**Insights**

Some features show strong correlation with house price.

These features will be useful for building prediction models.

Features with low correlation may contribute less to price prediction.

**Lot Size Related Correlation**
"""

plt.figure(figsize=(6,4))
sns.heatmap(data[['Lot_Size','Garage_Size','House_Price']].corr(),annot=True)
plt.show()

"""**Insights**

Lot size shows a relationship with house price.

Larger properties tend to have higher value.

Land size plays a role in real estate valuation.

**Year Built Correlation**
"""

plt.figure(figsize=(6,4))
sns.heatmap(data[['Year_Built','House_Price']].corr(),annot=True)
plt.show()

"""**Insights**

Year built affects house price through property age.

Newer houses often have modern features.

Older houses may require renovation.

#**Key Insights Summary**

* Square Footage has a strong positive
relationship with House Price.

* Houses with more bedrooms and bathrooms generally have higher prices.

* Better Neighborhood Quality leads to higher house prices.

* Larger lot size and garage size slightly increase house value.

* No significant outliers were found in the dataset.

#**Conclusion**

EDA helped understand the relationships between house features and house price. Important factors influencing price include Square Footage, Bedrooms, Bathrooms, and Neighborhood Quality. The dataset is clean and suitable for building machine learning models.

#**Recommendations**

* Use important features like Square Footage, Bathrooms, and Neighborhood Quality for prediction.

* Apply feature scaling before training models.

* Train multiple regression models to find the best performing model.

#**Handle Outliers**

**Check direction of outliers**
"""

fig,ax=plt.subplots(1,3,figsize=(12,3))

sns.boxplot(data,x='Square_Footage',ax=ax[0])
sns.boxplot(data,x='Lot_Size',ax=ax[1])
sns.boxplot(data,x='House_Price',ax=ax[2])
plt.tight_layout()

"""**Check distribution of lot_size and house_price**"""

fig,ax=plt.subplots(1,2,figsize=(12,3))
sns.histplot(data,x='Lot_Size',ax=ax[0])
sns.histplot(data,x='House_Price',ax=ax[1])
plt.tight_layout()

"""#**Check Skewness**"""

data['Lot_Size'].skew()

data['House_Price'].skew()

"""#**Find Outliers for House_Price**"""

Q1 = data['House_Price'].quantile(0.25)
Q3 = data['House_Price'].quantile(0.75)
IQR = Q3 - Q1
lower_limit = Q1 - 1.5 * IQR
upper_limit = Q3 + 1.5 * IQR

data[(data['House_Price'] < lower_limit) | (data['House_Price'] > upper_limit)]

len(data[(data['House_Price'] < lower_limit) | (data['House_Price'] > upper_limit)]) / len(data) * 100

"""#**Find Outliers for Square_Footage**"""

Q1 = data['Square_Footage'].quantile(0.25)
Q3 = data['Square_Footage'].quantile(0.75)

IQR = Q3 - Q1

lower_limit = Q1 - 1.5 * IQR
upper_limit = Q3 + 1.5 * IQR

data[(data['Square_Footage'] < lower_limit) | (data['Square_Footage'] > upper_limit)]

"""#**Detect Outliers Quickly for ALL Columns**"""

for col in data.columns:

    Q1 = data[col].quantile(0.25)
    Q3 = data[col].quantile(0.75)

    IQR = Q3 - Q1

    lower_limit = Q1 - 1.5 * IQR
    upper_limit = Q3 + 1.5 * IQR

    outliers = data[(data[col] < lower_limit) | (data[col] > upper_limit)]

    print(col, "Outliers:", len(outliers))

"""The IQR method was applied to all numerical columns.

No values fall outside the calculated lower and upper limits.

Therefore, no outliers were detected in the dataset.

Since the percentage of outliers is 0%, no outlier treatment such as removal or replacement is required.
The dataset appears to be clean and well distributed for further modeling.

#**Convert text to number**

No categorical features are present in the dataset; hence encoding is not applied.
"""

data

"""#**Scaling**
 Scaling brings all the numerical columns values into a same scale so that calculations becomes easy for ML algorithms

Scale ALL these columns:

*  Square_Footage
*  Num_Bedrooms
*  Num_Bathrooms
*  Year_Built
*  Lot_Size
*  Garage_Size
*  Neighborhood_Quality

#Min-Max scaler
"""

df1=data.copy()

from sklearn.preprocessing import MinMaxScaler
scaler=MinMaxScaler()
df1[['Square_Footage']]=scaler.fit_transform(df1[['Square_Footage']])
df1[['Num_Bedrooms']]=scaler.fit_transform(df1[['Num_Bedrooms']])
df1[['Num_Bathrooms']]=scaler.fit_transform(df1[['Num_Bathrooms']])
df1[['Year_Built']]=scaler.fit_transform(df1[['Year_Built']])
df1[['Lot_Size']]=scaler.fit_transform(df1[['Lot_Size']])
df1[['Garage_Size']]=scaler.fit_transform(df1[['Garage_Size']])
df1[['Neighborhood_Quality']]=scaler.fit_transform(df1[['Neighborhood_Quality']])

"""#Standard Scaler"""

#df2=data.copy()

# Split first
from sklearn.model_selection import train_test_split
x = data.drop(columns='House_Price')
y = data['House_Price']

x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.2, random_state=42)

# Apply scaling AFTER split
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()

x_train = scaler.fit_transform(x_train)
x_test = scaler.transform(x_test)

"""#**Transformation**"""

#Linear Regression expect target to be a normal
plt.figure(figsize=(5,2))
sns.histplot(data,x='House_Price',kde=True,color='purple')
plt.show()

"""Since the target variable House_Price shows an approximately normal distribution with no significant skewness, log transformation was not required.

#**Feature Engineering**
"""

import seaborn as sns
import matplotlib.pyplot as plt

sns.heatmap(data.corr(), annot=True)
plt.show()

"""All features are meaningful for predicting House_Price.

No feature is removed, all features are selected.

#**Split data for X and Y**
"""

x= df1.drop(columns='House_Price')
y = df1.loc[:,'House_Price']

"""#**Split data for training and testing**"""

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state=42)

data.shape

x_train.shape

x_test.shape

y_train.shape

y_test.shape

"""#**Model Building**"""

#import Linear Regression model
from sklearn.linear_model import LinearRegression
model=LinearRegression()
model.fit(x_train,y_train)

#predictions
y_pred=model.predict(x_test)

#slope
model.coef_

#intercept
model.intercept_

"""#**Evaluate the model**

* MAE (Mean Absolute Error)

* MSE (Mean Squared Error)

* RMSE (Root Mean Squared Error)

* MAPE

* R2 Score

* Adjusted r2
"""

from sklearn.metrics import mean_absolute_error,mean_squared_error,mean_absolute_percentage_error,r2_score

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""99% better model."""

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #number of input columns

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""The Linear Regression model achieved an R² score of 0.998, indicating an excellent fit. The low error metrics (MAE, RMSE, MAPE) confirm that the model predicts house prices with high accuracy. This is likely due to strong linear relationships among the features and the clean nature of the dataset.

#**KNN Regressor**
"""

from sklearn.neighbors import KNeighborsRegressor
model=KNeighborsRegressor(n_neighbors=3)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""80% better performance"""

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""The KNN Regressor achieved an R² score of 80, indicating good performance. However, compared to Linear Regression, it showed higher error values (MAE, RMSE, MAPE). This suggests that KNN is less suitable for this dataset, as the data exhibits strong linear relationships where Linear Regression performs better.

Why KNN performed worse?

 “Because the dataset is linear, and KNN works better for non-linear patterns.”

Why Linear Regression is better?

“Because features have strong linear relationship with target.”

#**Decision Tree Regressor**
"""

from sklearn.tree import DecisionTreeRegressor
model=DecisionTreeRegressor(max_depth=10,min_samples_split=2,min_samples_leaf=1)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

"""#Hyper Parameter

max_depth=10,min_samples_split=2,min_samples_leaf=1

criterion='squared_error',max_depth=6,max_features='sqrt',min_samples_split=5,min_samples_leaf=3
"""

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""98% model is better"""

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""The Decision Tree Regressor achieved an R² score of 0.984, indicating strong predictive performance. However, compared to Linear Regression, it showed slightly higher error values. This suggests that while Decision Trees can capture complex patterns, the dataset exhibits strong linear relationships where Linear Regression performs better.

Why Decision Tree not best?

“Because the dataset is linear and Decision Tree may overfit.”

Advantage of Decision Tree?

“It can capture non-linear relationships.”

After applying hyperparameter tuning, the model performance decreased significantly. This indicates that the model was underfitting due to excessive restrictions such as limited depth and feature selection. The default Decision Tree performed better, suggesting that the dataset requires a more flexible model.

Hyperparameters are the settings defined before training the model that control how the model learns. In a Decision Tree Regressor, important hyperparameters include max_depth, min_samples_split, min_samples_leaf, and max_features.

Proper tuning of these parameters is important to avoid overfitting and underfitting. In this project, it was observed that highly restrictive parameters (like low depth and limited features) led to underfitting and reduced performance. Therefore, default or moderately tuned parameters provided better results.

#**Cross validation**

#K Fold
"""

from sklearn.model_selection import cross_val_score
from sklearn.model_selection import KFold
model=DecisionTreeRegressor(criterion='squared_error',max_depth=6,max_features='sqrt',min_samples_split=5,min_samples_leaf=3)
kfold=KFold(n_splits=5,shuffle=True,random_state=42)
scores=cross_val_score(model,x_train,y_train,cv=kfold)

scores

print(np.mean(scores))

"""#Startified K Fold"""

from sklearn.model_selection import StratifiedKFold
model=DecisionTreeRegressor(criterion='squared_error',max_depth=6,max_features='sqrt',min_samples_split=5,min_samples_leaf=3)
sfold=StratifiedKFold(n_splits=5,shuffle=True,random_state=42)
scores=cross_val_score(model,x_train,y_train)

scores

print(np.mean(scores))

"""K-Fold Cross Validation was applied to evaluate model performance. The Decision Tree model showed inconsistent performance across folds with an average score of 0.61, indicating poor generalization. Stratified K-Fold was not suitable as it is used for classification problems. Hence, K-Fold was preferred for regression analysis.

#**Random Forest Regressor**
"""

from sklearn.ensemble import RandomForestRegressor
model=RandomForestRegressor(n_estimators=10,max_depth=6,max_features=5,bootstrap=True,min_samples_split=5,min_samples_leaf=3)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""99% model is better"""

p=x_test.shape[1]
p #

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""The Random Forest Regressor(bagging) achieved an R² score of 0.994, indicating excellent predictive performance. It outperformed Decision Tree and KNN models due to its ensemble nature, which reduces overfitting. However, Linear Regression slightly outperformed it, suggesting that the dataset has strong linear relationships.

#**Gradient Boosting Regressor**
"""

from sklearn.ensemble import GradientBoostingRegressor
model=GradientBoostingRegressor()
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""99% model is better"""

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""#**XGBoost Regressor**"""

from xgboost import XGBRegressor
model=XGBRegressor()
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

"""99% model is better"""

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""#**Hyper parameter turning**

#**Grid Search CV with gradient boosting**
"""

from sklearn.ensemble import GradientBoostingRegressor
from sklearn.model_selection import GridSearchCV
model=GradientBoostingRegressor()
parameters={'max_depth':[5,6,7],'n_estimators':[100,200,300],'learning_rate':[0.01,0.1,0.2]}
grid=GridSearchCV(estimator=model,param_grid=parameters,cv=5)
grid.fit(x_train,y_train)

print(grid.best_params_)

model=GradientBoostingRegressor(learning_rate=0.1,max_depth=5,n_estimators=300)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""#**Randomised search for gradient Boosting**"""

from sklearn.model_selection import RandomizedSearchCV
parameters={'max_depth':[5,6,7],'n_estimators':[100,200,300],'learning_rate':[0.01,0.1,0.2]}
model=GradientBoostingRegressor()
random=RandomizedSearchCV(estimator=model,param_distributions=parameters,cv=5,n_iter=10)
random.fit(x_train,y_train)

print(random.best_params_)

model=GradientBoostingRegressor(learning_rate=0.1,max_depth=5,n_estimators=300)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#MSE (Mean Squared Error)
mse=mean_squared_error(y_test,y_pred)
mse

#MAE (Mean Absolute Error)
mae=mean_absolute_error(y_test,y_pred)
mae

#RMSE (Root Mean Squared Error)
rmse=np.sqrt(mse)
rmse

#MAPE (mean absolute percentage erorr)
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#R2 Score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
#adj=1-(1-r2)(n-1)/(n-p-1)
n=x_test.shape[0]
n#number of samples in test data

p=x_test.shape[1]
p #

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""#**XGBOOST Grid Search CV**"""

from xgboost import XGBRegressor
model=XGBRegressor()
parameters={'max_depth':[4,5,6],'n_estimators':[10,50,100,200,300],'learning_rate':[0.1,0.01,0.2],'reg_alpha':[0.1,0.01,0.2,0.3],'reg_lambda':[1,5,8,10]}
grid=GridSearchCV(estimator=model,param_grid=parameters,cv=5)
grid.fit(x_train,y_train)

print(grid.best_params_)

model=XGBRegressor(learning_rate=0.1,max_depth=4,n_estimators=300,reg_alpha=0.1,reg_lambda=5)
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#mean squared error
mse=mean_squared_error(y_test,y_pred)
mse

#mean absolute error
mae=mean_absolute_error(y_test,y_pred)
mae

#root mean squared error
rmse=np.sqrt(mse)
rmse

#mean absolute percentage error
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#r2 score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
n=x_test.shape[0]
n

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""#**RandomizedSearch CV with XGBOOST**"""

model=XGBRegressor()
parameters={'max_depth':[4,5,6],'n_estimators':[10,50,100,200,300],'learning_rate':[0.1,0.01,0.2],'reg_alpha':[0.1,0.01,0.2,0.3],'reg_lambda':[1,5,8,10]}
random=RandomizedSearchCV(estimator=model,param_distributions=parameters,cv=5,n_iter=30)
random.fit(x_train,y_train)

print(random.best_params_)

model=XGBRegressor(reg_lambda=10,reg_alpha= 0.2,n_estimators= 200,max_depth= 4,learning_rate= 0.2)
model.fit(x_train, y_train)
y_pred = model.predict(x_test)

#mean squared error
mse=mean_squared_error(y_test,y_pred)
mse

#mean absolute error
mae=mean_absolute_error(y_test,y_pred)
mae

#root mean squared error
rmse=np.sqrt(mse)
rmse

#mean absolute percentage error
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#r2 score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
n=x_test.shape[0]
n

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""Hyperparameter tuning was performed using GridSearchCV and RandomizedSearchCV for Gradient Boosting and XGBoost models. The best parameters were selected based on cross-validation performance. Gradient Boosting achieved an R² score of 0.996, outperforming XGBoost. This indicates that model performance depends on the dataset, and more complex models do not always yield better results.

#**Support Vector Regressor**
"""

from sklearn.svm import SVR
model=SVR()
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#mean squared error
mse=mean_squared_error(y_test,y_pred)
mse

#mean absolute error
mae=mean_absolute_error(y_test,y_pred)
mae

#root mean squared error
rmse=np.sqrt(mse)
rmse

#mean absolute percentage error
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#r2 score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
n=x_test.shape[0]
n

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""The SVR model performed poorly with an R² score close to zero, indicating that it failed to capture the relationship in the dataset. This is likely due to the linear nature of the data and the default hyperparameters used. Hence, SVR is not suitable for this dataset.

#**Grid search CV**
"""

from sklearn.model_selection import GridSearchCV,RandomizedSearchCV
model=SVR()
parameters={'kernel':['linear','poly','rbf','sigmoid'],'C':[1,5,10,15],'gamma':[0.1,0.01,0.001]}
grid=GridSearchCV(estimator=model,param_grid=parameters,cv=5,scoring='r2')
grid.fit(x_train,y_train)

print(grid.best_params_)

model=SVR(C=15,gamma=0.1,kernel='linear')
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#mean squared error
mse=mean_squared_error(y_test,y_pred)
mse

#mean absolute error
mae=mean_absolute_error(y_test,y_pred)
mae

#root mean squared error
rmse=np.sqrt(mse)
rmse

#mean absolute percentage error
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#r2 score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
n=x_test.shape[0]
n

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""Support Vector Regressor was implemented and tuned using GridSearchCV. Although there was slight improvement in performance, the model still produced low R² scores and high error values. This indicates that SVR is not suitable for this dataset, likely due to the linear nature of the data and large target values.

#**Randomized search CV**
"""

model=SVR()
parameters={'kernel':['linear','poly','rbf','sigmoid'],'C':[1,5,10,15],'gamma':[0.1,0.01,0.001]}
random=RandomizedSearchCV(estimator=model,param_distributions=parameters,cv=5,n_iter=10,scoring='r2')
random.fit(x_train,y_train)

print(random.best_params_)

model=SVR(C=10,gamma=0.1,kernel='linear')
model.fit(x_train,y_train)
y_pred=model.predict(x_test)

#mean squared error
mse=mean_squared_error(y_test,y_pred)
mse

#mean absolute error
mae=mean_absolute_error(y_test,y_pred)
mae

#root mean squared error
rmse=np.sqrt(mse)
rmse

#mean absolute percentage error
mape=mean_absolute_percentage_error(y_test,y_pred)
mape

#r2 score
r2=r2_score(y_test,y_pred)
r2

#adjusted r2 score
n=x_test.shape[0]
n

p=x_test.shape[1]
p

adj=1-(1-r2)*(n-1)/(n-p-1)
adj

"""Support Vector Regressor was tuned using both GridSearchCV and RandomizedSearchCV. However, the model continued to perform poorly with low R² scores and high error values. This indicates that SVR is not suitable for this dataset, likely due to the linear nature of the data and scale of the target variable.

Key Observations:

Linear models work best due to strong linear relationships.

Ensemble tree models (Random Forest, Gradient Boosting, XGBoost) perform well but are slower.

KNN and SVR are unsuitable for this dataset.

#**Select the Best Performing Model**

**Best Model**: Linear Regression

Highest R² (0.998) and lowest error metrics.

Fast training and interpretable coefficients.

**Alternative: Gradient Boosting / XGBoost**

Very high R² (0.996–0.997)

Slightly slower but can handle small non-linearities.

**Conclusion:**

For this dataset, Linear Regression is sufficient due to linear relationships.

Use ensemble methods only if more complex patterns are expected.
"""